[project]
name = "another-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "logger",
]

[tool.uv.sources]
logger = { workspace = true }
